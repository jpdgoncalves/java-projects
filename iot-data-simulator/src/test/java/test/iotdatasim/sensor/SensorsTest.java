package test.iotdatasim.sensor;

/**
 * Test that ensures that all sensor simulators
 * output the expected values when given a certain
 * input (seed and a clock to get the current time).
 */
public class SensorsTest {

}
